<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PackageDeliverySystem" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#43a4fc</item>
        <item name="colorPrimaryVariant">#43a4fc</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.PackageDeliverySystem.NoTitleBar" parent="Theme.PackageDeliverySystem">
        <item name="android:windowNoTitle">true</item>
    </style>


    <style name="Theme.PackageDeliverySystem.Fullscreen" parent="Theme.PackageDeliverySystem">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.PackageDeliverySystem.ActionBar.Fullscreen
        </item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.PackageDeliverySystem.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>

    <style name="Theme.PackageDeliverySystem.NoTitleBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PackageDeliverySystem.NoTitleBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PackageDeliverySystem.NoTitleBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>